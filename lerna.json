{
  "$schema": "https://json.schemastore.org/lerna.json",
  "packages": ["packages/*"],
  "version": "0.0.7",
  "npmClient": "pnpm",
  "command": {
    "version": {
      "exact": true
    },
    "publish": {
      "npmClient": "pnpm",
      "allowBranch": ["master"],
      "registry": "https://registry.npmjs.org/"
    }
  }
}
